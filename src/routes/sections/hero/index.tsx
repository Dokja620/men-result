import { component$ } from '@builder.io/qwik';

export const Hero = component$(() => {
    return (
        <section id="hero">
            <h2>Résultat aux examens BEPC</h2>
        </section>
    )
});